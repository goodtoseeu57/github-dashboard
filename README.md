# github-dashboard

A terminal GitHub dashboard built in Rust with a custom approach, so you can shape it however you want.

This project is for building and experimenting with a GitHub dashboard in the terminal instead of following a fixed template.
